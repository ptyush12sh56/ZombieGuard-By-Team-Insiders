# ZombieGuard — Requirements
# Exact tools from Technical Approach slide (iDEA 2.0 PS9)

# ── Backend (FastAPI / Flask) ─────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
httpx==0.27.2

# ── Pydantic validation ───────────────────────────────────────
pydantic==2.8.2

# ── ML / AI (scikit-learn + NumPy) ───────────────────────────
scikit-learn==1.5.1
numpy==1.26.4
pandas==2.2.2

# ── BeautifulSoup — HTML/XML API doc ingestion ───────────────
beautifulsoup4==4.12.3
lxml==5.2.2
requests==2.32.3

# ── Database — SQL Alchemy (SQLite) ──────────────────────────
sqlalchemy==2.0.32

# ── Kafka (kafka-python) — streaming bus ─────────────────────
kafka-python==2.0.2

# ── TensorFlow/Keras — LSTM (optional, heavy) ────────────────
# tensorflow==2.17.0   # uncomment for full LSTM; NumPy sim used in POC

# ── Testing ───────────────────────────────────────────────────
pytest==8.3.2
pytest-asyncio==0.23.8
