# ZombieGuard — Windows-Compatible Requirements
# Use: pip install -r requirements_windows.txt

# ── Backend ───────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
httpx>=0.27.0

# ── Pydantic ─────────────────────────────────────────────────
pydantic>=2.6.0

# ── ML — pre-built wheels only (no compilation on Windows) ───
scikit-learn>=1.3.2,<1.6.0
numpy>=1.26.0,<2.0.0
pandas>=2.1.0

# ── BeautifulSoup ─────────────────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.1.0
requests>=2.31.0

# ── SQL Alchemy ───────────────────────────────────────────────
sqlalchemy>=2.0.0

# ── Kafka ─────────────────────────────────────────────────────
kafka-python>=2.0.2

# ── Testing ───────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
